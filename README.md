Netflix clone Back End application

# To create DB

install and run docker.
Run docker-compose up --build -d mongodb in mongo folder

# To start dev server

run - yarn dev or npm run dev

# To build server

run - yarn build or npm run build
